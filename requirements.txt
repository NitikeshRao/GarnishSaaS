# Asynchronous Server Gateway Interface - for async support in Django apps
asgiref==3.8.1

# For creating and validating dunder (__*) methods and attributes
attrs==24.2.0

# Django utility to configure DB connections via URLs (e.g., in env vars)
dj-database-url==2.3.0

# Main Django framework
Django==5.0.9

# Handles Cross-Origin Resource Sharing (CORS) headers in Django
django-cors-headers==4.6.0

# Backend for connecting Django to Microsoft SQL Server / Azure SQL
django-mssql-backend==2.8.1

# Provides password reset functionality via Django REST
django-rest-passwordreset==1.5.0

# Django REST Framework for building RESTful APIs
djangorestframework==3.15.2

# JSON Web Token authentication for Django REST Framework
djangorestframework-simplejwt==5.3.1

# Used for singular/plural and other word inflection transformations
inflection==0.5.1

# Configuration file parser used in test suites
iniconfig==2.0.0

# For validating JSON schemas
jsonschema==4.23.0

# Core JSON schema specifications (used with `jsonschema`)
jsonschema-specifications==2024.10.1

# WSGI HTTP server for running Python web apps in production
gunicorn==23.0.0

# Numerical computing library, used with pandas or independently
numpy==2.1.0

# OpenAPI specification library
openapi==2.0.0

# Excel reading/writing support
openpyxl==3.1.5

# Core package for parsing and handling versioning, semver, etc.
packaging==24.2

# Powerful data analysis and manipulation library
pandas==2.2.2

# Interactive shell features (e.g., autocomplete, history) — used in REPLs
prompt_toolkit==3.0.48

# PostgreSQL adapter for Python (used with Django + PostgreSQL)
psycopg2-binary==2.9.9

# Required for parsing C language code (used internally by some packages)
pycparser==2.22

# Python implementation for JWT encoding and decoding
PyJWT==2.10.0

# ODBC driver for connecting to MS SQL Server and other ODBC-compliant DBs
pyodbc==5.2.0

# Package Index utility — may be a misnamed or placeholder package
pypi==2.1

# For working with dates — commonly used with pandas
python-dateutil==2.9.0.post0

# Timezone utilities
pytz==2024.1

# YAML parser and emitter for Python
PyYAML==6.0.2

# Django debug toolbar — helpful during development
django-debug-toolbar==5.0.1

# Python client to interact with Vercel Blob Storage
vercel_blob==0.3.0

# Core library for Azure SDKs
azure-core==1.34.0

# HTTP library for Python (used internally by requests and Azure SDKs)
urllib3==2.2.2

# Timezone data (IANA) for Python tz-aware datetime support
tzdata==2024.1

# Azure AI Document Intelligence SDK for document extraction and analysis
azure-ai-documentintelligence==1.0.2

# Azure Form Recognizer SDK — extract structured data from forms
azure-ai-formrecognizer==3.3.3

# Common utilities shared across Azure SDK packages
azure-common==1.1.28

# Automated Swagger and Redoc documentation for Django REST APIs
drf-yasg==1.21.10

# Simple HTTP library — used in almost all web-related Python apps
requests==2.32.3

# SQL parser used internally by Django for formatting and introspection
sqlparse==0.5.1

# Python Imaging Library fork – used for image processing 
Pillow ==11.3.0

# Environment variable management for Django settings
django-environ ==0.12.0

drf-spectacular             ==  0.28.0
drf-spectacular-sidecar     ==  2025.8.1
django-browser-reload==1.18.0
mysqlclient==2.2.7
typing_extensions==4.13.2
